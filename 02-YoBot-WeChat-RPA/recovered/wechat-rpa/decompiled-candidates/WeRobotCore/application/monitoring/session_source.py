# UNVERIFIED DECOMPILER OUTPUT: may contain incorrect behavior.
# Source Generated with Decompyle++
# File: session_source.marshal (Python 3.9)

'''Session-list input boundary for the shared auto-reply monitor.

This is deliberately a narrow compatibility seam.  ``ChatMonitorV2`` still
owns all mature comparison, baseline, task-generation and broadcast rules;
drivers provide one normalized batch whose items retain the established
Legacy projection for consumers that have not migrated yet.
'''
from typing import Any, Optional, Protocol, TypedDict, runtime_checkable
from session_batch import MonitorSessionBatch
MonitorSessionPayload = <NODE:27>((lambda : isSelf: bool = 'Driver-neutral session facts required by the existing monitor.'), 'MonitorSessionPayload', TypedDict, False, **('total',))
MonitorSessionSource = runtime_checkable(<NODE:12>)
