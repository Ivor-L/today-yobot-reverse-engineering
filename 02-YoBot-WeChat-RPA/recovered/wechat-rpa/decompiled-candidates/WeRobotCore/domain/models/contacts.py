# UNVERIFIED DECOMPILER OUTPUT: may contain incorrect behavior.
# Source Generated with Decompyle++
# File: contacts.marshal (Python 3.9)

'''Normalized contact and group facts shared by contact providers.'''
from dataclasses import dataclass, field
from typing import Any, Mapping, Optional, Tuple
ContactRecord = dataclass(True, **('frozen',))(<NODE:12>)
GroupRecord = dataclass(True, **('frozen',))(<NODE:12>)
