// Compiled fragment from ./src/app/modules/shell/modules/request-headers/consts.ts.
// The original TypeScript and import graph are not restored.

const AUTHORITATIVE_HEADER_NAMES = new Set([
    'x-app-version',
    'x-client-platform',
    'x-traffic-lane',
    'x-vercel-protection-bypass',
    'x-vercel-set-bypass-cookie'
]);
const REQUEST_FILTER = {
    urls: [
        '<all_urls>'
    ]
};
