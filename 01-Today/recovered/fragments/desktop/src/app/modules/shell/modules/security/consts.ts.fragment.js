// Compiled fragment from ./src/app/modules/shell/modules/security/consts.ts.
// The original TypeScript and import graph are not restored.

const TODAY_ROOT_HOSTS = [
    'today.ai',
    'todayai.dev'
];
const OAUTH_POPUP_PATH = '/oauth/popup/preparing';
const OAUTH_POPUP_WINDOW_NAMES = new Set([
    'today_channel_oauth',
    'today_connector_oauth'
]);
const BRIEF_POPUP_WINDOW_NAME_PREFIX = 'today_brief_';
const CHILD_WINDOW_TITLE_BAR_HEIGHT = 48;
const PAYWALL_POPUP_WINDOW_NAMES = new Set([
    'today_billing_cancel',
    'today_billing_management',
    'today_pricing_checkout'
]);
