// Compiled fragment from ./src/app/modules/shell/modules/security/modules/artifact-windows/consts.ts.
// The original TypeScript and import graph are not restored.

const ARTIFACT_PREVIEW_WINDOW_NAME_PREFIX = 'today_artifact_';
const ARTIFACT_EXTERNAL_BROWSER_WINDOW_NAME = 'today_external_browser_from_artifact';
const ARTIFACT_WINDOW_OPTIONS = {
    autoHideMenuBar: true,
    backgroundColor: '#ffffff',
    center: true,
    fullscreenable: true,
    height: 700,
    maximizable: true,
    minHeight: 480,
    minWidth: 520,
    resizable: true,
    show: false,
    useContentSize: true,
    width: 900
};
