// Compiled fragment from ./src/app/modules/data-reset/modules/startup/modules/files/consts.ts.
// The original TypeScript and import graph are not restored.

// These belong to the new process's live singleton lease, not the old profile.
const SINGLE_INSTANCE_FILES = [
    'SingletonCookie',
    'SingletonLock',
    'SingletonSocket'
];

// EXTERNAL MODULE: ../../node_modules/.pnpm/yaml@2.8.3/node_modules/yaml/dist/index.js
var yaml_dist = __webpack_require__(1483);
