// Compiled fragment from ./src/app/platforms/_base/modules/platform-host-transport/modules/logger/index.ts.
// The original TypeScript and import graph are not restored.



class PlatformHostLogger {
    warn(message) {
        console.warn(message);
    }
}
PlatformHostLogger = __decorate([
    injectable()
], PlatformHostLogger);
