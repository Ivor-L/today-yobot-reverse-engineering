// Compiled fragment from ./src/consts/linux-dev.mjs.
// The original TypeScript and import graph are not restored.

// Process identity for the local AppImage launcher; never infer it from a PID alone.
const LINUX_DEV_INSTANCE_ARGUMENT = '--today-linux-dev-instance';
