// Compiled fragment from ../../packages/client-node-adapter/src/aspects/sei/modules/shortcuts/modules/chat-history-search/consts.ts.
// The original TypeScript and import graph are not restored.


const MACOS_CHAT_HISTORY_SEARCH_BINDING = Object.freeze({
    code: 'KeyK',
    modifiers: Object.freeze([
        (/* inlined export .ShortcutModifier.Meta */"meta")
    ])
});
const NON_MACOS_CHAT_HISTORY_SEARCH_BINDING = Object.freeze({
    code: 'KeyK',
    modifiers: Object.freeze([
        (/* inlined export .ShortcutModifier.Control */"control")
    ])
});
