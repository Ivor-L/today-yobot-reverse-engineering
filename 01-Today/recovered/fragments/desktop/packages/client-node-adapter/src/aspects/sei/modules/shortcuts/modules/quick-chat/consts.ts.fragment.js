// Compiled fragment from ../../packages/client-node-adapter/src/aspects/sei/modules/shortcuts/modules/quick-chat/consts.ts.
// The original TypeScript and import graph are not restored.


const MACOS_QUICK_CHAT_BINDING = Object.freeze({
    code: 'Space',
    modifiers: Object.freeze([
        (/* inlined export .ShortcutModifier.Alt */"alt")
    ])
});
const NON_MACOS_QUICK_CHAT_BINDING = Object.freeze({
    code: 'Space',
    modifiers: Object.freeze([
        (/* inlined export .ShortcutModifier.Control */"control"),
        (/* inlined export .ShortcutModifier.Alt */"alt")
    ])
});
