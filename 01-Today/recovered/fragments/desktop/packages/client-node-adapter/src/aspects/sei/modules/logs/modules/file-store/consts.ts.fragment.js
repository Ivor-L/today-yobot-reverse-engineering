// Compiled fragment from ../../packages/client-node-adapter/src/aspects/sei/modules/logs/modules/file-store/consts.ts.
// The original TypeScript and import graph are not restored.

const FILE_LOG_FILE_MODE = 384;
const DEFAULT_FILE_LOG_FLUSH_INTERVAL_MS = 3000;
const DEFAULT_FILE_LOG_MAX_AGE_MS = 3 * 24 * 60 * 60 * 1000;
const DEFAULT_FILE_LOG_MAX_BYTES = 350 * 1024 * 1024;
const DEFAULT_FILE_LOG_RETENTION_CHECK_LINE_COUNT = 300;
const DEFAULT_FILE_LOG_RETENTION_CHECK_INTERVAL_MS = 60 * 60 * 1000;
const FILE_LOG_SIZE_RETENTION_TARGET_RATIO = 0.9;
const DEFAULT_FILE_LOG_STALE_TEMPORARY_FILE_MAX_AGE_MS = 3 * 24 * 60 * 60 * 1000;
