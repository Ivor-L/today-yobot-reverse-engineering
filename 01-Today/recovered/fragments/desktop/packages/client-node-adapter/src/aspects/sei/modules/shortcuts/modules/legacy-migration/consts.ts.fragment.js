// Compiled fragment from ../../packages/client-node-adapter/src/aspects/sei/modules/shortcuts/modules/legacy-migration/consts.ts.
// The original TypeScript and import graph are not restored.

/**
 * Identifier persisted in `shortcuts.json` once the legacy macOS Quick Chat
 * choice has been considered. It is recorded even when nothing was imported, so
 * a restart, a later rebinding, or a reset to the platform default can never
 * re-run the import.
 */ const LEGACY_MACOS_QUICK_CHAT_MIGRATION_ID = 'legacy-macos-quick-chat';
