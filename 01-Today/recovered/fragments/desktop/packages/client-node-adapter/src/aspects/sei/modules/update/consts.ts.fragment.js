// Compiled fragment from ../../packages/client-node-adapter/src/aspects/sei/modules/update/consts.ts.
// The original TypeScript and import graph are not restored.

const UPDATE_CHECK_INTERVAL_MS = 6 * 60 * 60 * 1000;
const UPDATE_STARTUP_DELAY_MS = 30 * 1000;
const UPDATE_CHECK_ERROR_MESSAGE = 'Today could not check for updates. Try again later.';
const UPDATE_CANCELLED_MESSAGE = 'The update download was cancelled.';
const UPDATE_INSTALL_ERROR_MESSAGE = 'Today could not install the update. Try again later.';
