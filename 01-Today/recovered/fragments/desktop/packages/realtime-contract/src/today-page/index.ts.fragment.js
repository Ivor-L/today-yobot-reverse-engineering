// Compiled fragment from ../../packages/realtime-contract/src/today-page/index.ts.
// The original TypeScript and import graph are not restored.

const TODAY_PAGE_V2_BATCH_READY_PUSH_EVENT = 'today_page.batch_ready.v2';
/** No-payload invalidation signal. Clients refetch Today V3 batches when it arrives. */ const TODAY_PAGE_V3_UPDATES_PUSH_EVENT = 'today_page.v3.updates';
