// Compiled fragment from ../../packages/utils/src/function.ts.
// The original TypeScript and import graph are not restored.

const compute = (computation)=>computation();
const safe = async (operation)=>await operation();
