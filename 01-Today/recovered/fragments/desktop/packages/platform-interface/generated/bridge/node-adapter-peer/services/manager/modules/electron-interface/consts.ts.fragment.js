// Compiled fragment from ../../packages/platform-interface/generated/bridge/node-adapter-peer/services/manager/modules/electron-interface/consts.ts.
// The original TypeScript and import graph are not restored.

// Generated by scripts/generate.mts. Do not edit.


const UNTRUSTED_WEB_CONTENTS_ERROR = {
    code: base_InterfaceErrorCode.Unavailable,
    message: 'The Web interface is unavailable.'
};
const createWebExtendedDescriptor = (debugCapable, unsupportedModuleNames)=>{
    const descriptor = createInterfaceDescriptor(interfaceDefinitions.wei.kind, interfaceDefinitions.wei.modules);
    const unsupportedModules = new Set(unsupportedModuleNames);
    if (!debugCapable) {
        unsupportedModules.add('debug');
    }
    if (unsupportedModules.size === 0) {
        return descriptor;
    }
    return {
        ...descriptor,
        modules: descriptor.modules.map((moduleDescriptor)=>({
                ...moduleDescriptor,
                availability: unsupportedModules.has(moduleDescriptor.name) ? (/* inlined export .ModuleAvailability.Unsupported */"unsupported") : moduleDescriptor.availability
            }))
    };
};
