// Compiled fragment from ../../packages/platform-interface/generated/bridge/node-adapter-peer/services/manager/modules/electron-interface/utils.ts.
// The original TypeScript and import graph are not restored.

// Generated by scripts/generate.mts. Do not edit.


const parseEventTarget = (interfaceDefinition, value)=>{
    if (!lodash_es_isObject(value) || lodash_es_isFunction(value) || !('moduleName' in value) || !('eventName' in value)) {
        throw new TypeError('Invalid platform interface event subscription');
    }
    const moduleName = String(value.moduleName);
    const eventName = String(value.eventName);
    getEventDefinition(interfaceDefinition, moduleName, eventName);
    return {
        moduleName,
        eventName
    };
};
const redactEventPayloadForDebug = (moduleName, eventName, payload)=>{
    if (moduleName === 'shell' && eventName === 'deepLinkRequested') {
        return {
            url: '[REDACTED]'
        };
    }
    return payload;
};
